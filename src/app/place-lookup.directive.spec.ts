import { PlaceLookupDirective } from './place-lookup.directive';

describe('PlaceLookupDirective', () => {
  it('should create an instance', () => {
    const directive = new PlaceLookupDirective();
    expect(directive).toBeTruthy();
  });
});
